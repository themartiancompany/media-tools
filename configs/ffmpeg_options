_ffmpeg_opts=(
)
_loglevel="quiet"
_ffmpeg_opts+=(
  -loglevel
    "${_loglevel}"
  )
[[ "${quiet}" == "y" ]] && \
  _ffmpeg_opts+=(
    -y
  )
[[ "${quiet}" != "y" ]] && \
  _ffmpeg_opts+=(
    -stats
  )
_ffmpeg_opts+=(
  -hide_banner
)
